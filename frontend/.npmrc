# Hoist @radix-ui/* internal dependencies so Next.js Turbopack can resolve them.
# Without this, pnpm's strict isolation hides transitive deps from the bundler.
public-hoist-pattern[]=*@radix-ui/*
public-hoist-pattern[]=*react-remove-scroll*
public-hoist-pattern[]=*aria-hidden*


